#include <stdio.h>
#include <cs50.h>
#include <string.h>


int main(void)
{
    printf("Enter name: \n");
    string name = get_string();
    int full_name[];
    for (int i = 0, n = strlen(name); i < n; i++)
    {
        strncat(name[i] * full_name)
    }
    printf("namev: %c\n", full_name);
}